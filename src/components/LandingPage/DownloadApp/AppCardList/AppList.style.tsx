import { List, styled } from '@mui/material';

export const AppList = styled(List)(() => ({
  display: 'flex',
  flexDirection: 'row',
  height: '30%',
  justifyContent: 'center',
  alignContent: 'center',
  gap: '1.5rem',
  padding: '2rem',
}));
